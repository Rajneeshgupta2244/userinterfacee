import 'package:flutter/material.dart';
import 'package:storedata/splsscr.dart';

void main() {
  runApp(Store());
}

class Store extends StatelessWidget {
  const Store({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home:Splshpge() ,
    );
  }
}
